import { entity as userModel } from './user.models';
import { entity as NotificationModel } from './notifications.models';
import { entity as ThreadModel } from './threads.models';

export {
    userModel,
    NotificationModel,
    ThreadModel,
};